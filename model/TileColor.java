package model;

/**
 * Enum representing different colors for game tiles.
 * Each color corresponds to a specific type of game tile.
 */
public enum TileColor {
    RED,
    BLUE,
    GREEN,
    YELLOW,
    ORANGE,
    WHITE
}
