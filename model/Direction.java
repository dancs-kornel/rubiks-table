package model;

/**
 * Enum representing cardinal directions.
 * Each direction represents a possible movement in a grid-based game.
 */
public enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT
}
